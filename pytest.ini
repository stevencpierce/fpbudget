[pytest]
testpaths = tests
markers =
    admin: tests requiring admin privileges
    collab: multi-browser collaboration tests
    slow: tests that take a long time
addopts =
    -v
    --tb=short
    --alluredir=allure-results
filterwarnings =
    ignore::DeprecationWarning
